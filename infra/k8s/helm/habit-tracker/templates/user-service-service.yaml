apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: user-service
  ports:
    - port: 4001
      targetPort: {{ .Values.userService.port }}
      protocol: TCP
