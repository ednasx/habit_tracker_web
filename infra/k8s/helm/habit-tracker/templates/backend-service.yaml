apiVersion: v1
kind: Service
metadata:
  name: habit-backend
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: habit-backend
  ports:
    - port: 4000
      targetPort: {{ .Values.backend.port }}
      protocol: TCP
