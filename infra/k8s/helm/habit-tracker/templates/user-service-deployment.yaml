apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-service
  namespace: {{ .Values.namespace }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: user-service
  template:
    metadata:
      labels:
        app: user-service
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "{{ .Values.userService.port }}"
        prometheus.io/path: "/metrics"
    spec:
      containers:
        - name: user-service
          image: {{ .Values.userService.image }}
          imagePullPolicy: Always
          ports:
            - containerPort: {{ .Values.userService.port }}
          env:
            - name: NODE_ENV
              value: "{{ .Values.userService.nodeEnv }}"
            - name: SERVICE_NAME
              value: "{{ .Values.userService.serviceName }}"
            - name: SERVICE_VERSION
              value: "{{ .Values.userService.serviceVersion }}"
            - name: PORT
              value: "{{ .Values.userService.port }}"
            # Later: mount secrets for DB, Supabase, RabbitMQ, etc.
            - name: RABBITMQ_URL
              value: "amqp://habit-tracker-rabbitmq:5672"
            - name: RABBITMQ_HABIT_EXCHANGE
              value: "habit.events"
            - name: SUPABASE_URL
              valueFrom:
                secretKeyRef:
                  name: {{ .Release.Name }}-supabase
                  key: SUPABASE_URL
            - name: SUPABASE_SERVICE_ROLE_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ .Release.Name }}-supabase
                  key: SUPABASE_SERVICE_ROLE_KEY
            - name: SUPABASE_JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ .Release.Name }}-supabase
                  key: SUPABASE_JWT_SECRET
